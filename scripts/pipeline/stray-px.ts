// DELIBERATE VIOLATION, removed in the commit after this one. Class strings
// reach the page from scripts/ too, which is why the scan covers both roots.
export const STRAY_CLASS = 'mt-[12px] max-w-[600px]'
